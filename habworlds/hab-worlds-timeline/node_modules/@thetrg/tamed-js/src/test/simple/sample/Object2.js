tm.add ('test.simple.Object2', function (o, p, d) {
    o.setup = function (config) {
        if (!config.city) { config.city = 'Phoenix'; }
        d.city = config.city;
    }

    o.getCity = function () {
        return d.city;
    }
});
