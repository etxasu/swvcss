tm.add ('test.simple.ExtendFromObject', ['test.simple.Object1'], function (o, p, d) {
    o.setup = function (config) {
    }

    o.modifyName = function (namePrefix) {
        var name;
        name = namePrefix + ' ' + d.name;
        return name;
    }
});
