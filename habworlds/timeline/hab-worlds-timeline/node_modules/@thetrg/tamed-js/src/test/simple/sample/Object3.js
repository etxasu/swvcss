tm.add ('test.simple.Object3', function (o) {
    if (!d._config.city) { d._config.city = 'Phoenix'; }
    d.city = d._config.city;

    // NOTE: The setup method is mission on purpose.
    // o.setup = function () {
    // }

    o.getCity = function () {
        return d.city;
    }
});
